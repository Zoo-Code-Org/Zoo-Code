{
	"name": "@zoo-code/vscode-e2e",
	"private": true,
	"scripts": {
		"lint": "eslint src --ext=ts --max-warnings=0",
		"check-types": "tsc -p tsconfig.esm.json --noEmit",
		"format": "prettier --write src",
		"test:ci": "pnpm -w bundle && pnpm --filter @zoo-code/vscode-webview build && pnpm test:run",
		"test:ci:mock": "pnpm -w bundle && pnpm --filter @zoo-code/vscode-webview build && USE_MOCK=true pnpm test:run",
		"test:record": "AIMOCK_RECORD=true pnpm test:ci",
		"test:run": "rimraf out && tsc -p tsconfig.json && npx dotenvx run -f .env.local -- node ./out/runTest.js",
		"clean": "rimraf out .turbo"
	},
	"devDependencies": {
		"@zoo-code/config-eslint": "workspace:^",
		"@zoo-code/config-typescript": "workspace:^",
		"@zoo-code/types": "workspace:^",
		"@copilotkit/aimock": "^1.15.1",
		"@types/mocha": "^10.0.10",
		"@types/node": "20.x",
		"@types/vscode": "^1.95.0",
		"@vscode/test-cli": "^0.0.11",
		"@vscode/test-electron": "^2.4.0",
		"glob": "^11.1.0",
		"mocha": "^11.1.0",
		"rimraf": "^6.0.1"
	}
}
