---
"zoo-code": minor
---

Add the IO Intelligence (io.net) provider with a dynamically fetched model catalog, secret-state API key storage, and catalog-derived metadata (context window, output limits, vision and prompt-cache capability flags, per-million pricing).
