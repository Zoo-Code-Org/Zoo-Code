---
"zoo-code": patch
---

Hide the Run/Deny command approval buttons when a command is auto-executed (auto-approved or auto-denied) so the buttons don't stay visible during long-running auto-executed commands.
