---
"zoo-code": patch
---

Hide the Run/Deny command approval buttons when a command is auto-approved so the auto-execute path matches the manual click path, and the buttons don't stay visible during long-running auto-executed commands.
